// Q.1.Find if the conditions are obeyed or not?
//     You are given two number first as A and second as B and check if both conditions 
//     (A<10 and A>B) are satisfied or not with the help of operators.

// const AddTwoNumber=(a,b)=>{
//     return a+b;
// }
// console.log(AddTwoNumber(2,5));
  
// Q.2.Add two Numbers You are provided with two numbers A and B. 
  //      our task is to add these two numbers.

//   const Is_Valid = (a,b) => {
//     return(a<10 && a>b)?"true":"false";
//   };
//   console.log(Is_Valid(5,3));

// Q.3.Check the conditons
        // You are given two numbers A and B.You need to do the following checks:
        // 1.if both are divisible by 10 console true.
        // 2.if both are not divisible by 10 console false.
        // 3.if one of them only is divisible by 10 console true.
        // Use operator to do this.

    // const Check = (A, B) => 
    // {
    //     return ((A%10===0 && B%10===0) || (A%10===0 || B%10===0))?"true":"false";
    // };
    // console.log(Check(12,20));

// Q.4.Find the first digit of a 4 digit number
       //You are provided a four digit number N only. Your task is to print out the first digit of that number.

       
    // const First_Digit = (n) => {
    //     while (n >= 10)
    //             n /= 10;
    //         return Math.floor(n);
    // };
    // console.log(First_Digit(9767));

// Q.5.Find the last digit of a 4 digit number
      //You are provided a four digit number N only. Your task is to print out the last digit of that number.

    //   const Last_Digit = (n) => {
    //     return n%10;
    //  };
    //   console.log(Last_Digit(3478));   
    
// Q.6.Find the remainder
     //You are provided with two numbers A and B where A as  divisor and B as  dividend.Your task is find the remainder.

     
        // const Find_the_remainder = (a,b) => {
        //     let res = b%a;
        //     return res;
        // };
        // console.log(Find_the_remainder(10,19));

// Q.7.Multipy two Numbers
      //You are provided with two numbers A and B.Your task is to multiply these two numbers.


    //   const Multiply_two_number = (a,b) => {
    //     let res = a * b ;
    //     return res;
    //   };
    //   console.log(Multiply_two_number(5,3));    


//Q.8.Marks Calculator
      //Shyam has got his marks in three subjects as A,B and C (out of 100).Write a program to calculate his total marks and his total percentage.

    //   const Sum = (A, B, C) => 
    //   {
    //    return A + B + C;
    //   };
      
    //   const Average = (A, B, C) => 
    //   {
    //     let total = Sum(A,B,C);
        
    //      return (total / 3);
    //   };
    //   console.log(Sum(56,89,100));
    //   console.log(Average(56,89,100));














