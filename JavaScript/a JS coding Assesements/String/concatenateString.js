//Q.Concatenate the Strings
// You are given two strings S1 and S2 (containing both uppercase and lowercase 
// letters), You need to retrun a string which is the concatenation of both the given strings. 

var Concatenate_Strings = (S1, S2) => 
{
    let S3=(S1 + S2);
    return S3;
};
 console.log(Concatenate_Strings('nitin','kanhurkar'));
