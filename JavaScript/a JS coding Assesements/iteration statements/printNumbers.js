//Q.Print Numbers You are given an integer N, and your task is to print all the
     //integers starting from 1 till N  ( N inclusive)


     const printNumbers = (n) => {
        for(var i = 1 ; i <= n ; i++){
         console.log(i);
        }
    };
    printNumbers(10);